# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'

target 'ID_estudante' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftyJSON', '~> 4.0'

  # Pods for ID_estudante

  target 'ID_estudanteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ID_estudanteUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
